services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DEBUG: "True"
      SECRET_KEY: "docker-dev-only-change-for-real-use"
      ALLOWED_HOSTS: "localhost,127.0.0.1,0.0.0.0,[::1]"
